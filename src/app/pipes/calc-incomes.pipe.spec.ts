import { CalcIncomesPipe } from './calc-incomes.pipe';

describe('CalcIncomesPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcIncomesPipe();
    expect(pipe).toBeTruthy();
  });
});
