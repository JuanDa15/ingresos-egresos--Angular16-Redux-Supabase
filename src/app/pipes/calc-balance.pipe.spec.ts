import { CalcBalancePipe } from './calc-balance.pipe';

describe('CalcBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
