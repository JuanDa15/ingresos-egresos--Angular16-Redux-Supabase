import { ItemsQuantityPipe } from './items-quantity.pipe';

describe('ItemsQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
